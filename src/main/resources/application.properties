spring.application.name=amazon-project

#For Cloud Deployment Only
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#For local run only
#spring.datasource.url=jdbc:mysql://host.docker.internal:3306/ecom-project?createDatabaseIfNotExist=True
#spring.datasource.username=root
#spring.datasource.password=hello@123
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


jwt.secret=${JWT_SECRET}

server.port=8443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=${keystore_password}
server.ssl.key-password=${keystore_password}
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=ecomcert